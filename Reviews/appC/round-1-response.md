# Appendix C (Data, Code and Reproducibility) — Round 1 response

thesis-revision-writer, 2026-09-09. Files revised: `appendices/appC-data-code.tex` (wrapper),
`appC-usd-data.tex`, `appC-zar-data.tex`, `appC-code.tex`. Nothing under `thesis/code/` was
edited; the `certify` package and its README were read as the source of record for every
number and every function name in Section~C.4.

Report answered: `reviews/appC/round-1-review.md` (items C1–C10 must, C11–C21 should).

Counts: **DONE 19, DECLINED 0, DEFERRED 2** (one item, C7, is DONE-with-escalation and is
counted as DONE; see its entry).

---

## Must items

**C1. Describe the `certify` package. DONE.**
New `\subsection{The \texttt{certify} package}` (`subsec:appC-certify`, `appC-code.tex`),
about 1,000 words in the style of the `rates` paragraphs: one paragraph per function group
(blocking; threshold and certificate; the drift term; the blocking curve; comparison, not
theorem; the AR(1) example; tests). Every name written is a name that exists in
`certify/README.md` and `certify/__init__.py` — `blocks`, `deployment_start`,
`block_scores`, `conformal_k`, `conformal_threshold`, `beta_quantile`, `dkw_level`,
`coupling_cost`, `certificate`, `ecdf_gap`, `dK_hat`, `W1_hat`, `sqrt_drift_bound`,
`deficit_curve`, `optimal_block_length`, `optimal_deficit`, `balance_block_length`,
`expected_shortfall_bound`, the six `ar1_*` helpers, `simulate_ar1`,
`stationary_bootstrap_quantiles`, `fig_example.py`. Each paragraph names the label it
implements (`alg:prelim-blocking`, `def:score`, `def:prelim-split`, `thm:cov`,
`cor:dkw`, `lem:blocks`, `prop:noest`, `lem:w1area`, `prop:w1`, `lem:ar1`, `alg:cert`),
the dependency set (numpy, scipy for the Beta and normal quantiles), the run command
(`cd thesis/code && uv run --with numpy --with scipy python certify/tests/test_certify.py`,
seven tests), the seed 20260909 and what each test reproduces. Both invariants are written
as interface facts, not documentation: `beta_fn` is supplied and never estimated
(`prop:noest`), and the stationary bootstrap "is an empirical comparator and carries no
coverage statement".

Two accuracy corrections made in this round against `certify/__init__.py`:
`ar1_beta_bound` is described as taking $\varphi$ and returning the map
$k\mapsto\tfrac12\sqrt{-\log(1-\varphi^{2k})}$ in the form `certificate` expects for
`beta_fn` (it is a closure, not a two-argument function), and `simulate_ar1` is described
as drawing from a supplied innovation standard deviation to which the caller passes
$\sqrt{1-\varphi^2}$ (the normalisation is the caller's, not the function's).

**C2. Make the package regenerate Table 3.1 and quote one AR(1) constant. DONE.**
The code was corrected before this text was written: one mixing constant
(`ar1_beta_bound` = Chapter 3 `lem:ar1`, first inequality), the block count fixed
(trailing gap inside the record, $250+240+10=500$ at $N=500$, $b=10$), and
`test_table_3_1` added. The appendix now takes every cell from the README rather than from
hand arithmetic. Closing `\datanote{}` of `subsec:appC-certify` records the regenerated
cells — $\beta(b)$, $(n+1)\beta(b)$, $\dK$, marginal level, $b_{n,k}(0.05)$,
$\beta(b)/\delta'$, conditional level, confidence at $b\in\{5,10,20\}$, and
$\E F_Q(\hat q^*)=0.90889$ — states that Table~\ref{tab:example} and Figure~\ref{fig:example}
are regenerated by `test_table_3_1` and `fig_example.py` at seed 20260909, and states that
every cell agrees with the digits printed in Chapter 3, so no hand-arithmetic discrepancy
remains. The three valid $\beta(10)$ bounds ($4.883\times10^{-4}$ from `lem:ar1`,
$5.638\times10^{-4}$ from the $k$-uniform form, $6.905\times10^{-4}$ from
`prop:prelim-ar1`) are listed with their sources and explicitly not corrected into one
another. Chapter 3's constant was not touched from here.
Also in this round: the figure paragraph now quotes the certified breach excess over
$\alpha$ exactly as the README gives it ($0.925$, $0.129$, $0.130$ at $\varphi=\tfrac12$;
$8.72$, $1.50$, $0.186$ at $\varphi=0.8$) instead of naming the bound without values.

**C3. USD citations. DONE.**
The section carries seven `\gap{Citation needed: ...}` boxes and five `\datanote{}`s, one
per group of facts, and no unsourced fact stands bare:
- `appC:usd-calendar`: FCA 5 Mar 2021, ISDA 5 Mar 2021, FCA 3 Jul 2023 and Sep 2024,
  CFTC MRAC 8409-21/8449-21 — gap box, "None of these documents has been obtained and none
  has a bibliography entry"; the dates stand as unverified until Chapter 1 cites the texts.
- `appC:usd-spread`: gap box for Regulation ZZ. **The July 2022 link is now named as the
  proposal, not the final rule**: "the only Board document located for it, the
  press-release attachment of 19 July 2022, is the *proposed* rule and not the final one,
  so the five values in Table~\ref{tab:usd-spread} rest on no obtained primary source and
  the Federal Register citation of the final rule must be read before they are cited". The
  BISL fact-sheet tickers, the two-day shift and the licence-fee waiver are in the same box.
  A separate `\datanote{}` records that 1W and 2M are outside the LIBOR Act and that the
  BISL notice rendered as images only, and that neither tenor is used.
- `appC:usd-ois`: gap box for the NY Fed page and Terms of Use, the FRED removal notice,
  the ICE user agreement and the USD 5,000 fee, the CME listing date.
- `appC:usd-libor-vol`: the "documented in the literature" sentence is **replaced by a gap
  box** stating that no work was named and no source is on disk, and that the thesis relies
  only on the vendor quoted/derived flag for that period; the "ATM 3M forward about 0.2%"
  claim is a `\datanote{}` marked unverified.
- `appC:usd-sofr-vol`: the Clarus, Risk.net and Deriscope material is confined to one
  `\datanote{}` headed "Secondary sources, unverified" which states that no fact in the
  thesis rests on them. The gap box adds ISDA Supplement 88, the Settlement Matrix, the ICE
  Swap Rate guidance, the 2021 Definitions date, CME Term SOFR and the ARRC recommendation.
  In this round the same box records the status of the one live citation: `brace2024` is
  cited only for the qualitative feature that SOFR forwards jump at scheduled policy dates,
  the article was not obtained, it carries no locator, and no quantitative claim rests on it
  (the Highlights note for `brace2024` says in terms that no locator may yet be written).
- `appC:usd-basis`: gap box for the CME and LCH conversion notices and for the four RICs,
  which "remain TBC until confirmed in Workspace".
The SOFR print of 17 September 2019 is attributed to the NY Fed series in the same
subsection as that series.

**C4. ZAR citations. DONE.**
`bisl2025` is attached to the CAS lead sentence, the table caption and the 3M value, and
in this round each carries the locator the Highlights note records: `\citep[p.~1]{bisl2025}`
for the fixing date and `\citep[p.~1, Table]{bisl2025}` for the five values and the five
`YJIBA` tickers. The stale note ("no Highlights note exists ... carries no page locator")
is replaced by a `\datanote{}` describing the document itself: a two-page technical note
marked "for information purposes only", whose single table page is the locator, and which
does **not** state the five-year median methodology — which is why `sarb2025fallback` and
not this note is cited for the methodology. `isda2025` carries `[p.~1]` and `[Sect.~1.1]`,
`sarb2025ois` carries five section/page locators, `sarb2025fallback` three and
`sarb2024conv` two, all matching the recorded notes. Missing primaries are gap-boxed: the
SARB ZARONIA page and statement of methodology (publication time, republication window,
start date, three history classes); the SARB JIBAR page and the JSE portal; the SARB and
JSE terms of use. The LCH 11 April 2026 date is a `\datanote{}` stating the member updates
have not been obtained and that Chapter 1 cites it once the notice is read; the LCH
clearing and Bloomberg SEF claims are in the same class of `\datanote{}` marked unverified.
The unnamed "commercial commentary" quotation is gone; the only commercial provider named
(BlueGamma) is named, marked not primary, and marked not relied on.

**C5. Licence terms per ZAR dataset. DONE.**
`tab:appC-zar` now has six columns, "Dataset, Class, Source, Coverage, Licence, In package",
with a licence entry for every row: SARB website terms with redistribution marked TBC; SARB
copyright with "link, not copy" for the `.xlsb` workbooks; JSE market-data terms marked TBC;
"public note; five values quoted" for the BISL spreads; terminal agreement for the BISL
daily series and the live OIS curve; "data agreement; aggregates only" for the sponsor
surfaces. The "In package" column takes the four protocol values (yes / script + hash /
hash-only / no) and matches C6 item 4 row for row. The gap box after the table states that
until the SARB and JSE terms are settled those rows ship the download script, the selector
settings and the SHA-256 hash, not the files.

**C6. Reproducibility protocol. DONE.**
`\section{Reproducibility protocol}` (`sec:appC-protocol`) replaces the `\gap{}` with the
nine numbered paragraphs the report specified: (1) `Code/` at the repository root and the
path convention; (2) one environment, Python 3.12+, numpy, scipy for `certify` only, both
run commands, and the versions verified on 9 September 2026; (3) seeds, in
`tab:appC-seeds` — result, script, seed, replications, runtime, six rows, deterministic rows
marked; (4) the manifest fields, including the Reserve Bank selector settings (range
requested, earliest date returned, download timestamp) and the four in-package flags;
(5) verification with and without shipped data, including the vendor-revision case where a
hash mismatch is reported and not resolved silently; (6) the quoted/derived/interpolated
flag and the $\pm100$ bp rule; (7) expected outputs and tolerances (five significant
figures for Chapter 2, four decimals for every cell of Table 3.1, $10^{-9}$ for the
effective-parameter closed forms, $10^{-12}$ for the clock identity, three standard errors
for Monte Carlo); (8) sponsor data, pointing to the governance paragraph; (9) the
generative-AI record, with the categories and the verification rule. Two `\gap{}` boxes
mark what is genuinely not yet fixed: the archive of record and its identifier, and the
version pin itself. `\subsection{Terminal session checklist}` (`subsec:appC-checklist`)
closes C19.

**C7. Bring the appendix to budget. DONE for the listed cuts; residual escalated as the
report directs.**
All eight cuts (a)–(h) are made: (a) the "Two consequences for Chapter 6" passage is gone;
(b) the vendor paragraph triplets are compressed in this round — `appC:usd-ois`,
`appC:usd-libor-vol`, `appC:usd-sofr-vol` and `appC:usd-basis` now give one sentence each on
what is not available and defer identifiers to `tab:usd-register`, into which the dropped
identifiers were moved (`BBIR`, `HP`/`BDH`, LSEG `NormalizedVolatility`, the
`USDSR3L{3M..50Y}=TPSR` points, `SWPM`/`ICVS`, `SOFR75`, `SOFRVOL`, the NY Fed endpoint
path); (c) the three copies of the terminal-licence sentence are replaced by one
"Licensing principles" paragraph in the appendix introduction; (d) the `sec:appC-pnl`
method passage is reduced to the five-item input list with sources; (e) the synthetic-curve
passage keeps only the prohibition (never estimate $\eta$, $\rho_{BF}$, $\rho_{B\alpha}$
from it); (f) the public-data fallback is one sentence pointing to Chapter 7; (g)
`tab:usd-calendar` is deleted and the transition record referred to Chapter 1; (h) the
quality-issue enumerations are compact prose. In this round two further single-source cuts
were made in `appC-code.tex`: the worked-example number dump that reprinted
Section~\ref{subsec:prelim-worked} is reduced to what the test asserts and to what
tolerance, and the calendar-time substitution that is derived in Chapter 2 is referred to
rather than re-derived.

Measured length. In the build of record the appendix now runs printed pages 129–156, i.e.
**28 pages** (introduction 1, USD 8.5, ZAR 6, code 8, protocol 4.5), against 22 pages at
the time of review and a 16-page share. The report's own arithmetic accounts for this: the
same report requires C1 (+1.5), C5 (+0.3), C6 (+2), C12 (+0.5), C13 (three figure plans),
C19 (+0.5) and C20 (+0.3), and the seven gap boxes and thirteen data notes that C3 and C4
require are the largest single block of new vertical space in the appendix. Sixteen pages
is not reachable while C1–C6 stand: the two must-items are in conflict, and the conflict is
resolved in favour of the citation, code-description and protocol items, which are
correctness requirements, against the page target, which LESSONS records as ours and not a
Wits rule. **Reallocation request to the page-budget auditor: 12 pages for Appendix C, from
Appendix B (30 budgeted) or Chapter 7 (20 budgeted for a chapter the programme principles
cut to five thousand words).** The register tables and the minimum-viable-data paragraph
were not cut, per the report's instruction.

**C8 / C16. Chapter 6 and 7 design decisions removed. DONE.**
Every passage named is now a one-sentence pointer, and the data constraint is stated
separately from the decision:
- `appC:usd-calendar`: "The transition record (dates, spreads, conventions) is kept in
  Chapter~\ref{ch:intro}, and Chapter~\ref{ch:usd} fixes its fitting window $\Dfit$ and its
  deployment window from that calendar alone. The data constraints those choices must
  respect are three ...". **The repo-spike justification is deleted** (C16): the window is
  no longer justified by what is inside it, and the September 2019 print appears only as a
  data fact in `appC:usd-ois`, where the SOFR series is described.
- `appC:usd-libor-vol`: "Chapter~\ref{ch:usd} decides which part of that range is fitted
  and which is reported as a diagnostic of a dying market"; the SOFR-first fact is "a data
  fact that Chapter~\ref{ch:usd} takes into account when it fixes $\Dfit$".
- `appC:usd-sofr-vol`: "Chapter~\ref{ch:usd} fixes its reporting rule from that date" and
  "Chapter~\ref{ch:usd} states its treatment of short-expiry caplets that span a meeting".
- `appC:usd-basis`: "Chapter~\ref{ch:usd} fixes the estimation window for $\eta$ with this
  constraint".
- `sec:appC-pnl`: "Chapter~\ref{ch:zar} defines the policy, the block length and the
  fallback when no sponsor signs", followed by the five inputs and their sources only.
- The public-data fallback is one closing sentence deferring to Chapter 7.
The displaced design content is recorded below as lessons for the ch6 and ch7 writers.

**C9. `optimal_block_length` and the deficit. DONE.**
`appC-code.tex`, "The blocking curve" paragraph: `deficit_curve` is described as the
expected calibration-conditional shortfall bound of `thm:cov`(ii);
**`optimal_block_length` returns the exact minimiser
$b^*=(2(r+1))^{2/(2r+3)}N^{3/(2r+3)}$** and `optimal_deficit` its value
$\frac{2r+3}{2(r+1)}(2(r+1))^{1/(2r+3)}N^{-r/(2r+3)}$; the bare balance $N^{3/(2r+3)}$ is
named as available separately under `balance_block_length`, is stated not to be the
minimiser, and the two are separated numerically ($23.93$ against $14.35$ at $N=500$,
$r=2$, a factor $1.67$), with the sentence "No function in the package returns the balance
under a name that could be read as optimal". The former `deficit_actual` is described under
its corrected name `expected_shortfall_bound` with the constants
$1/(2\sqrt{n+2})+(n+1)\beta(b)$, and the appendix records that its AR(1) minimum at $b=11$
is a property of geometric mixing rather than an instance of $b^*$.

**C10. References to code that does not exist. DONE.**
The `subsec:appC-rates` sentence promising calibration code "in the sections that follow"
is gone; the paragraph now reads "a reference implementation ... not a production library:
it has three modules and no calibration routine". The unwritten modules are in one `\gap{}`
at the end of `subsec:appC-certify`: `strip`, `calibrate` and `scores` "are scheduled for
programme months 5--9 and 12--17; nothing in this appendix or in
Chapters~\ref{ch:usd} and~\ref{ch:zar} may be read as describing code that exists", with
the three recorded limitations of `certify` itself. In this round the stripping sentence in
`appC:usd-libor-vol` was rewritten to name the module as not written and to point at that
gap box: "the caplet-stripping module, which is not written
(Section~\ref{subsec:appC-certify}), will interpolate in strike only within the quoted
range" (it previously pointed at `subsec:appC-rates`, which describes a package that
contains no such module).

## Should items

**C11. One access-class taxonomy, one licensing paragraph. DONE.** `\paragraph{Access
classes.}` in `sec:appC-intro` defines F (free public download), T (university Bloomberg or
LSEG terminal), S (sponsor-only) and C (computed by the thesis code) once; both register
tables use it and both captions say "Classes F, T, S, C as defined in
Section~\ref{sec:appC-intro}". `\paragraph{Licensing principles.}` states the four
principles once — terminal data are never shipped, public data are shipped where the terms
allow, quoting a regulation or a technical note is quotation and not use of a licensed
service, sponsor data enter only as the three aggregates — and no dataset repeats them.

**C12. Appendix introduction. DONE.** `sec:appC-intro`: the three roles, the status line
("No market dataset has been downloaded and no terminal has been queried ... The code covers
Chapters~\ref{ch:prelim} to~\ref{ch:sharpness} only"), the access-class legend, the
licensing principles and the rule that design decisions live in Chapters 6 and 7.

**C13. Three planned figures. DONE.** `fig:appC-usd-timeline` (USD availability 2018–2024,
five series with the quoted/derived/absent distinction, four vertical dates, the windows
shaded, claim tested named in the caption); `fig:appC-zar-timeline` (ZAR 2005–2027, the
three ZARONIA history classes, JIBAR to cessation, synthetic and live curves, sponsor
window, monthly score record, three vertical dates); `fig:appC-modules` (three-column module
diagram, implemented against scheduled, arrows to the thesis objects each module computes).
All three captions name axes, series, source and the claim.

**C14. ZAR caption honesty. DONE.** "as recorded on 9~September 2026; rows marked TBC are
unverified".

**C15. ZAR score count and feasibility. DONE, and recorded for Chapter 7.**
`sec:appC-pnl` closing `\datanote{}`: the count is stated as arithmetic and not data, with
its assumption named (month 0 = proposal approval, record from May 2026); then
$n=\lfloor N/(2b)\rfloor$, a finite threshold requires
$k=\lceil(1-\alpha)(n+1)\rceil\le n$, i.e. $n\ge(1-\alpha)/\alpha$, so $n\ge9$ at
$\alpha=0.1$, hence $N\ge18$ at $b=1$ and $N\ge36$ at $b=2$: **on about twenty-four monthly
scores only $b=1$ yields a finite threshold at ten per cent**, and Chapter 7 must then
justify a one-month gap from its mixing class. In this round the phrase "as the ground truth
anticipates" was removed under the invisible-apparatus rule. A lesson for the ch7 writer is
appended below.

**C17. Chapter 2 erratum. DONE.** The `subsec:appC-rates` closing `\datanote{}` now reads
"The test asserts the closed forms quoted in the gap box after
Proposition~\ref{prop:prelim-nueff}, and the effective-parameter figures above are the
values the code returns for them", and keeps the run command and the environment line. The
correction itself is recorded as a lesson for the Chapter 2 reviser and is not asserted here.

**C18. Hard-coded numbers and the preamble hack. DONE (labels); DEFERRED (macros).**
No literal chapter, theorem or section number remains in any of the four files (checked by
grep for `Chapter~[0-9]`, `Theorem~[0-9]`, `Section~[0-9]`, and by confirming zero `??` in
the Appendix C pages of the build of record). `sec:appC-pnl` uses `\ref{def:score}`,
`\ref{thm:cov}` and `\ref{ch:zar}`. The `\makeatletter\g@addto@macro\UrlBreaks{...}` line
is removed from `appC-zar-data.tex`. **DEFERRED:** it has not been added to `macros.tex`,
which this writer does not own, and neither `macros.tex` nor `main.tex` loads `xurl`; the
long Reserve Bank and JSE URLs are held in `\begin{quote}\footnotesize\raggedright` blocks
and set without error in the current build, so the effect is cosmetic. Handed to the
macros owner as a one-line preamble addition (`\usepackage{xurl}` or the `\UrlBreaks`
line).

**C19. Closing out the TBC count. DONE.** `subsec:appC-checklist` lists exactly what the
single terminal session must return: the first quoted SOFR month on the volatility cube;
the cap and swaption ticker roots for 3M LIBOR and compounded SOFR with their strike and
expiry encoding; the SOFR OIS strip identifiers and curve number; the basis-curve history
depth and tenor points; the 1W and 2M spreads; the front SOFR futures identifier; the LIBOR
fixing identifier and its depth; and, for South Africa, the live ZARONIA OIS and JIBAR
curve identifiers, the JSE tenor columns and the earliest date the Reserve Bank selector
returns. Each return enters the manifest with the session date and clears one register
entry.

**C20. Sponsor-data governance. DONE.** `\paragraph{Governance of sponsor data.}` after the
four-item list in `sec:appC-vols`: custody on university-managed encrypted storage with
named access; retention to the end of candidature plus examination and revision, then
recorded destruction; examiner inspection of raw quotes under the same confidentiality
terms, "so that a restriction on publication is not a restriction on examination"; no
personal information, so POPIA is not engaged; the three permitted aggregates as the only
outputs. A `\gap{}` follows for the university research-data and intellectual-property
policies, which are not yet identified by name and version.

**C21. AI-assistance record. DONE.** Protocol item 9: the categories (code development,
presentation and editing, citation formatting), the verification rule (every number by a
run in `tab:appC-seeds` or by independent arithmetic in the data note of the chapter that
prints it; every citation against the cited document; no mathematical statement on tool
output alone, "which is why unproved statements appear as proof routes with an explicit list
of what remains to be established"), and a `\gap{}` for the tool names, versions and dates
and their mapping to the Faculty declaration categories.

## Items deferred

1. **C18, second half — the `\UrlBreaks` line into `macros.tex`.** Deferred to the owner of
   `macros.tex`; out of this writer's file ownership. One line, either `\usepackage{xurl}`
   after `hyperref` or the `\g@addto@macro\UrlBreaks` redefinition.
2. **C7 residual — 12 pages of budget.** Deferred to the page-budget auditor as the
   reallocation request stated in the C7 entry above. Nothing further can be cut without
   undoing C1, C3, C4, C5 or C6.

Nothing was declined.

---

## New `\gap{}` boxes in this appendix

Fourteen in all, all of them statements of what is not established or not obtained, none of
them narrating a draft:

`appC-data-code.tex` (3)
1. Archive of record for the code package, its persistent identifier and its release
   licence, and the corresponding sentence in Chapter 8 — not yet minted.
2. The version pin itself (a `requirements.txt` with exact versions and a lock file) — not
   yet in the package.
3. Tool names, versions and dates of AI use and their mapping to the Faculty declaration
   categories.

`appC-usd-data.tex` (7)
4. FCA and ISDA announcements of 5 March 2021, FCA news of 3 July 2023 and September 2024,
   CFTC MRAC releases — none obtained, no bibliography entry.
5. Regulation ZZ **final rule** for the five spread values and the ISDA-equivalence
   statement; the located July 2022 document is the proposal.
6. NY Fed publication page and Terms of Use, FRED removal notice, ICE user agreement and
   fee schedule, CME listing date.
7. The "off-ATM swaption quotes largely missing, August 2019 to April 2020" claim — no work
   named, no source on disk; the thesis relies only on the vendor flag for that period.
8. ISDA Supplement 88, Settlement Matrix v3.0, ICE Swap Rate guidance, 2021 Definitions
   effective date, CME Term SOFR, ARRC recommendation, CME SOFR options listing; and the
   status of `brace2024` (not obtained, no locator, no quantitative claim resting on it).
9. CME and LCH conversion notices; the four LSEG RICs, unconfirmed because forum posts are
   not primary documents.
10. (in the same section) the BISL fact sheet v7.2 for every ticker of `tab:usd-spread`,
    the two-day shift and the licence terms — carried inside box 5.

`appC-zar-data.tex` (3)
11. SARB ZARONIA benchmark page and statement of methodology — publication time,
    republication window, start date, three history classes; methodology not obtained.
12. University research-data management and intellectual-property policies under which the
    sponsor agreement is signed.
13. SARB website terms of use and JSE client-portal market-data terms, to settle
    redistribution.

`appC-code.tex` (1)
14. `strip`, `calibrate` and `scores` are not written and are scheduled for programme
    months 5–9 and 12–17; plus the three recorded limitations of `certify` ($\dK$ is an
    input; the dependent-data rate for $\hat\Wone$ is not implemented; `blocks` discards a
    tail shorter than $2b$).

## Length

| | Words | Printed pages |
|---|---|---|
| At round-1 review | not recorded by the panel | 22 (pp. 125–146) |
| At the point this revision resumed | 9,538 | 28 (pp. 129–156) |
| After this revision | 9,572 | 28, unchanged to the page in the last build; the cuts of C7(b) and the two single-source cuts in `appC-code.tex` remove about 300 words of prose, and about 340 words of citation status, licence terms and exact certified-excess values were added |

The growth from 22 to 28 pages is content the report required: the `certify` subsection
(C1), the licence and in-package columns (C5), the protocol section and its seed table (C6),
the appendix introduction (C12), three figure plans (C13), the terminal checklist (C19) and
the governance paragraph (C20), plus fourteen gap boxes and thirteen data notes from C3 and
C4. Word count is the honest measure of the writing; the page figure is the measure the
budget uses, and it is over by 12. See the C7 entry for the reallocation request.
