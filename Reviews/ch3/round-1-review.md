# Chapter 3 — Finite-Sample Certification of Hedging Error — round-1 review

Panel: thesis-review-panel, 2026-09-09. Perspectives adopted in turn: conformal, mixing, minimax, empirical-process, honesty, examiner; the zero-hallucination citation rule applied throughout.

File reviewed: `/Users/thabangbaloyi/Desktop/PhD Thesis/Council Workspace/thesis/chapters/ch3-certification.tex` (756 lines, 15,203 words of prose and display, 18 `proof` environments, 3 `figplan`, 1 table, 4 `datanote`, 1 `gap`).

Cross-read in full: `/Users/thabangbaloyi/Desktop/PhD Thesis/Council Workspace/13-mathematics-3yr.tex` status via GROUND-TRUTH.md corrections 1–16; `references/STANDARD.md`, `LESSONS.md` (all 304 lines, in particular the fifteen `thesis-reviewer-conformal` entries and the `thesis-code-conformal` entries 70–76, 268–273), `LOOP.md`, `CONTENT-MAP.md`; `chapters/ch2-preliminaries.tex` (current, post-revision state); `appendices/appB-proofs-standard.tex`; `reviews/ch2/round-1-review.md` (section 8 in particular); stubs `chapters/ch4-sharpness-blocking.tex` and `chapters/ch6-usd-validation.tex`; and the Highlights notes `adams2010`, `allohibi2026`, `barber2023`, `barber2026`, `dedecker2017`, `halkiewicz2026`, `oliveira2024`, `ramos2026`, `schmitt2026`, `stocker2025`, `vovk2012`, `xu2025`, `yu1994`, `zheng2024`, `zwart2025`.

Every numerical claim in the chapter was recomputed independently by this panel: `v_10 = 26.0039`, `sqrt(v_10) = 5.0994`, `dK = 0.0391`, `k = 24`, `k/(n+1) = 0.9231`, `beta(10) <= 4.883e-4`, `(n+1)beta(b) = 0.0127`, marginal `0.8482`, `b_{25,24}(0.05) = 0.8239` (the polynomial `u^24(25-24u)` returns `0.05002` there), `beta(b)/delta' = 0.0098`, conditional `0.7750`, confidence `0.9378`, union bound `0.8878`, DKW form `0.6552`, Gaussian tail `0.7531`, moment-matched normal `0.8387`, drift slack `0.9231 - 0.9089 = 0.0142`, realised band `[0.8962, 0.9216]`; and every cell of Table 3.1 at `b = 5, 20` (`v_5 = 11.125`, `v_20 = 56.0000`, `0.0299`, `0.0533`, `0.7971`, `0.0730`, `0.8262`, `0.4837`, `0.1686`, `0.7791`, `0.7258`, `0.9500`) and the `phi = 0.8` figures (`0.0538`, `1.40`, `0.00576`, `0.0749`). All agree to the digits printed. The `beta((X_t, X_{t+N}))` computation in Proposition `prop:noest` returns `(1-eps)/2` exactly as stated, on the `dTV <= 1` normalisation the thesis uses. Hoeffding's `Lambda''(lambda) = theta(1-theta)` and the `lambda = 4t` substitution are correct. **No arithmetical or algebraic error was found anywhere in the chapter.** The defects below are of attribution, positioning, single-source compliance and completeness.

---

## 1. Verdict

**MAJOR.**

The mathematics is right: the coupling count `(n+1)beta(b)`, the disintegrated coefficient with the Markov term `beta(b)/delta'`, the `sigma(D_fit, S_{1:n})`-measurable event on which part (ii) is stated, the closed half-line, the atom-safe Beta domination, the `sqrt(2 L W_1)` bound with its attaining counterexample, and the non-estimability construction all survive line-by-line checking, and every reported number reproduces. What the chapter does not yet do is obey the rules the thesis is being written under: thirteen results with a home in Chapter 2 or Appendix B are restated and re-proved here (Berbee in three places), which is where roughly eleven of the chapter's fifty-one typeset pages go; six passages narrate the drafting process, which the invisible-apparatus rule forbids outright; and three attributions required by the zero-hallucination rule are missing or wrong, including the published source of Proposition `prop:w1` and a false claim that Zheng and Proutière do not optimise their thinning parameter. The chapter is also incomplete in one doctoral respect: it proves only a lower bound on coverage, while the object Chapter 4 must minimise is the two-sided calibration-conditional error, which has no statement here to be minimised.

None of this is a rewrite. The spine — Assumption, Theorem 6, its three-step proof, Proposition 7 with its counterexample, Proposition `prop:noest`, the algorithm, the worked example — stands and should not be touched except where named below.

---

## 2. Re-check of previous round

Omitted: this is round 1.

---

## 3. Numbered items

Line numbers are those of the file as reviewed. One direct edit was made by this panel (item 5); it is described there.

### Substantive mathematics, honesty and structure

**1. [ADD] must — Section 3.3 "The certificate" (`sec:cert-theorem`), immediately after Theorem `thm:cov` and before Remark `rem:betaapprox`.**
Add a proposition giving the **two-sided** counterpart of both forms of the certificate, and a two-sentence remark on why it is needed. Content: (a) marginal upper bound `Prob(S_0 <= q̂) <= 1 - alpha + 1/(n+1) + dK(P,Q) + (n+1)beta(b)`, proved from the same coupling event plus the upper half of the exchangeable rank bound, which is `gamma <= Prob(S_{n+1} <= q̂_{n,gamma}) < gamma + 1/(n+1)` in `\citep[Thm.~1]{ramos2026}`; (b) conditional upper bound `F_P(q̂) <= b_{n,k}(1-delta)` on an event of probability at least `1-delta-n beta(b)`, which is the upper tail of the same Beta domination and costs two lines given Corollary `cor:exact`(a) run in the other direction (the domination is an equality in law when `F_P` is continuous, and only that case is needed for the upper bound to be non-vacuous — say so); (c) hence `|F_P(q̂) - (1-alpha)| <= max{1-alpha-b_{n,k}(delta), b_{n,k}(1-delta)-(1-alpha)} + dK + beta(b)/delta'` on an event of probability at least `1-2delta-delta'-n beta(b)`. About 3/4 of a page including the proof.
Why: GROUND-TRUTH correction 11 fixes the minimax object of Theorem 9 as the **two-sided** calibration-conditional coverage error `|F_P(T) - (1-alpha)|` at confidence `delta` over `B_r`, and records that the one-sided deficit over all procedures is degenerate (`T = +infinity`). Chapter 4 therefore has to optimise a quantity that Chapter 3, as drafted, never states. Without this proposition the doctoral theorem of the thesis has no antecedent in the chapter that is supposed to supply its tool, and an examiner reading Chapters 3 and 4 in order will ask where the two-sided object came from. `\citep[Thm.~3]{barber2023}` is the precedent for an over-coverage companion bound in the conformal-under-drift literature and should be cited as such (locator confirmed in the `barber2023` Highlights note); `\citep[Thm.~2]{halkiewicz2026}` is the precedent for the two-sided coverage **error** as the object of a minimax statement. Priority **must**.

**2. [FIX] must — Section 3.2 "The assumption", Assumption `ass:mix` (line 116), and the `\gap{}` box at line 394 (delete the box entirely).**
Restate the assumption on the state process: "The state process `(X_t)` of Section~\ref{sec:cert-policy}, which generates both the fitting sample and the per-period hedging errors, is `beta`-mixing with coefficients `beta(k)` in the sense of Definition~\ref{def:prelim-mixing} of Chapter~\ref{ch:prelim}, and the blocks `X_{H_1},...,X_{H_n}` have a common law. The fitting sample `D_fit`, the `n` calibration blocks and the deployment block are pairwise separated by gaps of at least `b` periods." Keep the "Reading of the assumption" paragraph at line 120 as the mathematical explanation of why the hedging-error process of the *fitted* policy does not inherit the coefficients, but delete from it any reference to what was "kept verbatim". Then delete the whole `\gap{}` box: item (1) is closed because the Berbee proof leaves the chapter under item 6 below and Appendix~\ref{app:proofs} Section~\ref{appB:berbee} already carries the measurable-selection details; item (2) is closed because the assumption is being rewritten; item (3) is closed because the approximation already sits in Remark `rem:betaapprox`.
Why: GROUND-TRUTH corrections 15 and 16 have both been made — correction 15 now reads that the mixing assumption "is placed on the market STATE process generating both the fitting sample and the hedging errors, not on the error process alone", and that "Theorem 6(ii) no longer contains the normal approximation to the Beta quantile or the '2.3 times' factor; those are a remark". The chapter is arguing against a version of the source of record that no longer exists, and it does so in the reader's face. Priority **must**.

**3. [FIX] must — Section 3.3.1 "Step 1", Corollary `cor:coupled` (line 213), in conjunction with item 6.**
Chapter 2's Lemma `lem:prelim-yu` (ch2 line 990) delivers only *independent copies with the right marginals*. Corollary `cor:coupled` needs two strictly stronger properties that only Chapter 3's own `lem:blocks`(a) currently supplies: that the **first** coupled object equals the original (`X*_{D_fit} = D_fit`, so that `pi(D_fit)` passes through the coupling unchanged), and that each `X*_{H_j}` is independent of the **actual** `D_fit`, not merely of the coupled predecessors. Without both, the sentence "Conditionally on `D_fit` ... the variables `S*_1,...,S*_n` are i.i.d. with law `P_pi`" does not follow, and the whole conditional argument collapses. Two acceptable resolutions, in order of preference: (a) request that the Chapter 2 reviser add the clause "moreover `X_1^* = X_1`, and for each `j` the copy `X_j^*` is independent of `(X_1,\dots,X_{j-1})`" to Lemma `lem:prelim-yu` and to its Appendix B proof, which the sequential-Berbee construction already delivers, and then have Chapter 3 cite it; or (b) if (a) is refused, keep in Chapter 3 a four-line corollary deriving exactly those two clauses from `lem:prelim-berbee` applied sequentially, and nothing more — not the full proof of `lem:blocks`.
Why: this is the one place where the repetition cuts of items 6–18 can silently break the mathematics. Flag it before, not after, the deletion. Priority **must**.

**4. [REMOVE] must — Section 3.8 "The bootstrap and breach-count backtests" (`sec:cert-backtests`, lines 701–750, about 1,740 words, 5.8 typeset pages).**
First words of the passage: "The certificate is not the only tool a bank can use to bound its hedging error." Move Sections 3.8.1 (block bootstraps, including the explicit stationary-bootstrap recipe and the `p ~ n'^{-1/3}` choice), 3.8.2 (Kupiec and Christoffersen statistics with their displays), 3.8.3 (Basel traffic light and the P&L attribution test, with its `\datanote{}` on the unverified zone boundaries) and 3.8.4 ("What Chapter 6 will measure") verbatim into `chapters/ch6-usd-validation.tex`, which is a two-line stub that no one else is editing, under a `\section{Methods compared}` with a `\gap{}` noting that the section is placed and not yet integrated. Retain in Chapter 3 exactly **one paragraph**, in the Discussion, saying: the certificate's level is a proved finite-sample statement under stated assumptions; the bootstrap's is asymptotic, with Edgeworth coefficients that depend on the mixing rate through constants that are not available, so no theorem comparing the two under Assumption~\ref{ass:mix} is claimed; the empirical comparison is Chapter~\ref{ch:usd}'s.
Why: the single-source rule gives every algorithm, market fact and dataset description exactly one home, and Chapter 6 is the home of the empirical comparison (GROUND-TRUTH: "First-draft bootstrap theorem DROPPED; empirical comparison only", Chapter 6). Every object in Section 3.8 — `kunsch1989`, `politis1994`, `lahiri2003`, `kupiec1995`, `christoffersen1998`, `bcbs2019` — is used *only* by Chapter 6. Chapter 3 gains nothing from describing them and loses six pages of its budget; and the unverified Basel zone numbers currently sit in a theory chapter, where an examiner will read them as established. This also removes `bcbs2019` from a chapter with no Highlights note for it. Priority **must**.

**5. [FIX] must — Section 3.9 "Discussion", Chapter notes (line 756). DIRECT EDIT MADE BY THIS PANEL.**
The sentence "`\cite{zheng2024}` split a Markov chain into `K` thinned subsequences ... and `\cite{allohibi2026}` use a three-way split with buffer gaps ...; **neither optimises the block length** or proves a lower bound" is false about `zheng2024`. Their Theorem 5.1 states an optimised thinning parameter `K*` (logarithmic in `n`) and Proposition 5.1 gives the two-sided interval with cost (retained points) × `beta(K)`. The panel has rewritten the sentence in place to: `\cite[Prop.~5.1]{zheng2024}` ... "and do optimise the thinning parameter, obtaining a `K^*` that is logarithmic in the sample size `\cite[Thm.~5.1]{zheng2024}`; `\cite{allohibi2026}` use a three-way split with buffer gaps under geometric `beta`-mixing. Neither works over a polynomial mixing class, neither gives a calibration-conditional statement, and neither proves a lower bound; and ...". The reviser must check the new sentence reads with the rest of the paragraph and must carry the same correction into any Chapter 4 or Chapter 1 text that repeats the claim.
Why: a false negative claim about a published paper is the kind of thing an external examiner in this exact area checks first, and the honest novelty of Chapter 4 does not need it — the real distinctions are the polynomial class, the calibration-conditional object and the lower bound, all three of which are now stated. Locators from the `zheng2024` Highlights note. Priority **must**.

**6. [CITE] must — Section 3.4.2 "The drift bound", Proposition `prop:w1` (line 439), Remark `rem:counter` (line 454), and the Chapter notes list of additions (line 756).**
The first inequality of Proposition `prop:w1`, `F_P(x) - F_Q(x) <= sqrt(2 L W_1(P,Q))` under a density bound on `Q` alone, is published. Cite it on the proposition as `\citep[Prop.~1, p.~4]{xu2025}`, where it is attributed to Ross (2011); `ross2011` is already a key in `bibliography.tex`, so cite both. Add one sentence after the statement: "The one-sided inequality is `\citep[Prop.~1, p.~4]{xu2025}`, attributed there to `\citet{ross2011}`; what is proved here is the specialisation to the certificate together with the two-sided refinement `dK <= sqrt(L W_1)` under a density bound on both laws, the `Winf` bound, and the sharpness of the constant." Then strike "the drift bound of Proposition~\ref{prop:w1} with its counterexample" from the Chapter-notes list of what the chapter adds, replacing it with "the two-sided refinement and the sharpness of the constant in Proposition~\ref{prop:w1}".
Why: GROUND-TRUTH correction 14 records the attribution ("Prop 7's `sqrt(2 L W1)` inequality is Ross (2011) and Xu et al. (2025) Prop. 1, specialised not claimed"), Chapter 2's Example `ex:prelim-w1-dk` already says so and already promises that Chapter 3 "claims neither the inequality nor its power" — and Chapter 3 as drafted claims both. Locator confirmed in the `xu2025` Highlights note. Priority **must**.

**7. [REMOVE] must — six passages, throughout.**
The invisible-apparatus rule forbids any mention of the council, the source-of-record document, the lessons file, drafts, rounds, versions or the writing process. Delete or rewrite, in each case keeping only the mathematics:
 (a) line 147, Remark `rem:betaapprox`, last sentence — first words "The ground-truth statement of this theorem carries the approximation".
 (b) line 394, the whole `\gap{}` box — see item 2.
 (c) line 455, Remark `rem:counter`, first sentence — first words "The first draft of this proposition claimed". Replace with "The obvious linear bound `|F_P(x)-F_Q(x)| <= L W_1(P,Q)` is false."
 (d) line 491, paragraph "The `n^{-1/4}` rate", last sentence — first words "The first draft's linear bound would have given". Replace with "A linear bound, were one available, would give `n^{-1/2}`; Remark~\ref{rem:counter} shows it is not."
 (e) line 716, Section 3.8.1, last two sentences — first words "The first draft of this thesis stated a theorem". These leave the chapter with item 4; the honesty content ("no theorem comparing them is claimed") is kept, the narration is not.
 (f) line 698, the `\datanote{}` after Table 3.1 — the clauses "Every number in the table and in the text above was recomputed independently on 2026-09-09", "must not be ``corrected'' into one another", and "The code is to be extended ... before submission". See item 14 for what replaces them.
Why: STANDARD, "Invisible apparatus" (binding); the Chapter 2 round-1 panel found about twenty such references in Chapter 2 and the same rule applies here. An examiner reads a scholarly text, not a change log. Priority **must**.

**8. [FIX] must — the chapter abstract (line 4), the Introduction (line 22 and the map at line 30), and the sentence at line 143 in Section 3.3.**
Three sentences assert that the chapter restates and proves the standard tools, and become false the moment items 6–18 are carried out. Rewrite: abstract, "The proof is written in full from three standard tools, each restated" becomes "The proof is written in full from three tools established in Chapter~\ref{ch:prelim}: Berbee's coupling applied sequentially, the Beta law of conformal coverage in the form that allows atoms, and a half-line comparison of two laws." Line 143, "Each borrowed result is stated in full before it is used" becomes "Each borrowed result is used with a reference to its statement in Chapter~\ref{ch:prelim}." Delete the three self-justifying sentences that accompany the deleted proofs: "The proof is given here in the form needed, because the whole certificate rests on it" (line 157); "The proof is repeated here in the general form, with atoms, because the conditional form of the certificate rests on it and the score law of a hedging record need not be continuous" (line 228); "The sharper bound below is proved here, by the same route, because the example needs the smallest number the route gives" (line 593, and note that Chapter 2's Proposition `prop:prelim-ar1` now carries exactly this sharper bound with no side condition, so the sentence is also factually stale).
Why: single-source rule; and the sentences are the chapter's own justification for the duplication being removed. Priority **must**.

**9. [FIX] must — Section 3.3.2, paragraph "The DKW form as a corollary" (line 275), against Section 3.5.3 "What is estimable" (line 559).**
Line 275 states "the uniform DKW inequality itself is not used anywhere in the thesis". Line 559 twice relies on it: "any functional of `P` with a finite-sample confidence bound under i.i.d. sampling, such as its distribution function by the DKW inequality", and (line 553) "the constant `J_1(P)` ... can be bounded from the same sample by the DKW inequality, at a further cost in `delta''`". Resolve one way: keep the two uses in Section 3.5, which are legitimate and cheap, and change line 275 to "the uniform DKW inequality is not used in the proof of Theorem~\ref{thm:cov}; it is used only in Section~\ref{sec:cert-estimable}, to bound a functional of the calibration law."
Why: two statements in one chapter cannot both stand; an internal contradiction of this kind is exactly what an internal examiner marks. Priority **must**.

**10. [FIX] must — Section 3.5.1 "The mixing coefficient is not estimable" (title, line 519), Proposition `prop:noest` title, and the paragraph after its proof (line 549).**
Three changes. (a) The paragraph currently reads "The result is of the type established by `\cite{adams2010}` for the ergodic class", which credits Adams and Nobel with a result of this type. They prove a *positive* uniform Glivenko–Cantelli theorem; the only sentence of theirs that bears on non-estimability is "the convergence guaranteed by the ergodic theorem can be arbitrarily slow and we cannot hope to obtain distribution-free probability bounds", at `\citep[Sec.~1.1]{adams2010}`. Rewrite as: "Proposition~\ref{prop:noest} is the finite-sample form of a familiar obstruction. Under ergodicity alone there is no distribution-free rate at all: `\citet[Thm.~1]{adams2010}` prove that every class of sets of finite Vapnik–Chervonenkis dimension is Glivenko–Cantelli for every stationary ergodic process, but note that the convergence `can be arbitrarily slow and we cannot hope to obtain distribution-free probability bounds' `\citep[Sec.~1.1]{adams2010}`. What Proposition~\ref{prop:noest} adds is that the mixing coefficient itself, the rate at which the process forgets, is invisible in a finite record." (b) Insert the qualifier **distribution-free** everywhere the blanket claim is made: the section title becomes "The mixing coefficient is not estimable distribution-free"; the abstract's "The chapter establishes that the mixing coefficient is not estimable from data" becomes "... is not estimable distribution-free from any finite record"; Section 3.1.2's "Section~\ref{sec:cert-estimable} proves that `beta(b)` is not estimable from any finite record" gains the same qualifier. (c) Add one sentence after the proposition: "Under further assumptions on the process the coefficient can be estimated; the proposition rules out only a bound that holds over all stationary processes with the observed `N`-marginal." No citation is needed for (b) or (c): both are weakenings, and the mathematics of Proposition `prop:noest` supports them exactly as stated.
Why: the `adams2010` Highlights note is explicit — "Cite this paper for `no distribution-free rate under ergodicity alone', not for `beta(b) is not estimable'". GROUND-TRUTH correction 16 has already been amended in the same direction. The reviser should **not** cite McDonald, Shalizi and Schervish (2015) as the counterpoint for (c): it is not in `bibliography.tex`, no PDF or Highlights note exists on disk, and the sentence as drafted above needs no source. Priority **must**.

**11. [FIX] must — Section 3.6, second paragraph after Algorithm `alg:cert` (line 585), and Section 3.9 "Connection to Chapter 4" (line 753).**
Both places write "`b^* \asymp N^{3/(2r+3)}`". Replace by the exact minimiser in both: "minimised at `b^* = (2(r+1))^{2/(2r+3)} N^{3/(2r+3)}`", and in the Discussion add the numeric anchor already established for the thesis, "which is `23.93` at `N = 500`, `r = 2`, against the bare balance value `14.35`". Also change "the calibration-conditional deficit `sqrt(b/N) + N b^{-(r+1)}`" to "the expected calibration-conditional shortfall `sqrt(b/N) + N b^{-(r+1)}`".
Why: GROUND-TRUTH correction 10 — "Quote the exact minimiser `(2(r+1))^{2/(2r+3)} N^{3/(2r+3)}`, never the bare balance `N^{3/(2r+3)}`" — and the same correction fixes the object as the expected calibration-conditional shortfall from Theorem 6(ii). The Chapter 2 panel raised the identical item against Chapter 2 (its item on forward references to Chapter 4); Chapter 3 must not reintroduce the older wording. Priority **must**.

**12. [CITE] must — Section 3.1 (line 22), Section 3.9 Chapter notes (line 756), and wherever else Theorem `thm:cov`(i) is positioned.**
`halkiewicz2026` is cited nowhere in the chapter. Add one sentence to the Chapter notes: "A second lower bound exists in this literature and is drift-driven rather than mixing-driven: `\citet[Thm.~4]{halkiewicz2026}` prove a minimax lower bound `c T^{-beta/(2beta+1)}` on the two-sided marginal coverage error over a class of drifting laws, with the hardness produced by a Gaussian mean bump over the last `m` observations, and optimise a rolling window by balancing a `1/sqrt(m)` noise term against a `(m/T)^beta` drift term `\citep[Sect.~3.4, eq.~(6)]{halkiewicz2026}`. The certificate's drift term is an assumption rather than a class, and the lower bound of Chapter~\ref{ch:sharpness} is mixing-driven; neither result implies the other."
Why: GROUND-TRUTH correction 12 is explicit — "Cite Barber and Pananjady (2026) and Halkiewicz (2026) wherever Theorem 9 or Theorem 6(i) is discussed. Halkiewicz's lower bound is drift-driven; Theorem 9(iii) is mixing-driven. Never claim the first optimal-design result for conformal prediction under dependence." Locators confirmed in the `halkiewicz2026` Highlights note. Priority **must**.

**13. [CITE] must — Section 3.3.2, Lemma `lem:beta` and Corollary `cor:dkw` (lines 226, 293), surviving as one-sentence reminders under items 11 and 13 of the repetition list.**
Two locators must appear where the Beta law and its Hoeffding corollary are invoked. The Beta law: `\citep[Prop.~2b, p.~479]{vovk2012}` for the exact conditional-coverage law in binomial form, together with `\citep[eq.~(4), p.~2]{zwart2025}` for the `Beta(k, n+1-k)` statement in the thesis's own notation — and the text must say "the Beta form of Vovk's Proposition 2b", never attribute the word "Beta" to Vovk, who does not use it. The Hoeffding-form lower bound on the quantile that Chapter 3 calls "the DKW form": this is exactly `\citep[Prop.~2a, p.~478]{vovk2012}`, whose statement is `E >= eps + sqrt(-ln delta/(2n))`; cite it, and stop presenting the bound as something the chapter derives that DKW would have given. The reference to `\cite{dkw1956}` "with the sharp constant of `\cite{massart1990}`" should be replaced by a cross-reference to Theorem `thm:prelim-dkw` of Chapter 2 alone: neither `dkw1956` nor `massart1990` has a Highlights note on disk, so no locator for either is available to this chapter, and Chapter 2 is their home in any case.
Why: zero-hallucination rule; locators confirmed in the `vovk2012` and `zwart2025` Highlights notes; the librarian's `zwart2025` note independently records that "DKWM (Massart, 1990) and Hoeffding (Hoeffding, 1963) bounds yield `eps = O(sqrt(ln(1/delta)/n))`", i.e. the bound is prior art and not a corollary the chapter is establishing. Note also that `hoeffding1963` has **no source on disk**: the locator `[Thm.~2]` currently used at line 279 cannot be verified from a Highlights note, so route the citation through Chapter 2's Lemma `lem:prelim-hoeffding` (which carries `\cite{hoeffding1963}` without a locator) rather than repeating the locator here. Priority **must**.

**14. [FIX] must — Section 3.7, the `\datanote{}` after Table 3.1 (line 698).**
The datanote is stale. `LESSONS` records that the certificate code now implements the first inequality of the AR(1) bound and that `test_table_3_1` reproduces every cell of Table 3.1 at `mu = 0.05` for `b` in `{5, 10, 20}`, so the clauses "the constant used by the certificate code of Appendix~\ref{app:data}, whose test case also sets `mu = 0` and therefore reports a marginal level `0.8853` and a conditional level `0.8126` with confidence `0.9359`" and "The code is to be extended to the drift case and the tighter constant so that it regenerates this table before submission" are both false as of now. Replace the whole three-bounds paragraph with: "The table uses the first inequality of Proposition~\ref{prop:prelim-ar1} of Chapter~\ref{ch:prelim}, `beta(k) <= (1/2) sqrt(-log(1-phi^{2k}))`, which gives `4.883e-4` at `phi = 1/2`, `k = 10`; the `k`-uniform third inequality of the same proposition gives `5.638e-4` and is the constant quoted when a class `{beta(k) <= C |phi|^k}` is needed. The certificate code of Appendix~\ref{app:data} regenerates every cell of this table." Keep the `v_5`, `v_20`, `dK` and Beta-quantile arithmetic, which this panel has verified. Also add, per the same lessons, the variance convention: "the AR(1) is normalised to stationary variance `v = 1`, so the innovation standard deviation is `sqrt(1-phi^2)`" — the figure and the code both depend on it and a reader cannot reproduce the numbers without it.
Why: two sets of numbers for one example is an examiner catch, and the datanote currently asserts a discrepancy that has been resolved. Priority **must**.

**15. [CITE] must + [ADD] — Section 3.4.4, paragraph "The deployment side is dependent" (line 496).**
Two changes. (a) Give the locators: `\citep[Prop.~3.2, eqs.~(3.7)--(3.8)]{dedecker2017}` for the general dependent empirical-`W_1` bound, `\citep[Remark~3.4(1)]{dedecker2017}` for `||W_1(mu_n, mu)||_2 << n^{-1/2}` under their condition (3.1), and `\citep[Remark~3.4(2), eq.~(3.12)]{dedecker2017}` for the polynomial-decay case `alpha(k) = O(k^{-a})`, `a > 1`, which is the case matching `B_r` with `a = r`; state that their class is Rio's weak `alpha`-dependence through half-line indicators, which contains `beta`-mixing, and that the constant must be assembled from the quantile integrals of (3.12), finite for bounded scores. (b) Replace the closing sentence "Until it is in place, the plug-in is an estimate whose deployment-side error is not certified" with a proper `\gap{}` stating the missing statement in mathematical terms and nothing else: "\gap{Not established here: a bound `E W_1(Q_m, Q) <= c_r m^{-1/2}` for the empirical law of `m` overlapping deployment blocks of a process in `B_r`, with `c_r` explicit in `(C, r)` and in the quantile function of the score law. Chapter~\ref{ch:sharpness} supplies it by specialising `\citep[Remark~3.4(2), eq.~(3.12)]{dedecker2017}`; until then Corollary~\ref{cor:plugin} certifies the calibration side only.}"
Why: zero-hallucination rule (the chapter currently says "Dedecker–Merlevède obtain rates" with no locator at all) and STANDARD's rule that a `\gap{}` states only what remains to be established, in mathematical terms. Locators confirmed in the `dedecker2017` Highlights note. Priority **must**.

### Repetition: results whose home is Chapter 2 or Appendix B

Every item below is must-priority under the single-source rule. In each case: delete the environment and its proof, keep **at most one sentence** of reminder carrying the cross-reference, and re-point every `\ref` to the Chapter 2 label. The Chapter 2 reviser has already adopted the Beta law **with atoms** (Theorem `thm:prelim-beta` now states stochastic domination and proves the randomised probability-integral-transform step) and Chapter 3's AR(1) bound (Proposition `prop:prelim-ar1` now gives `beta(k) <= (1/2) sqrt(-log(1-phi^{2k})) <= |phi|^k/(2 sqrt(1-phi^{2k})) <= |phi|^k/(2 sqrt(1-phi^2))` with no side condition), so items 11 and 21 are unblocked and both objects must go from Chapter 3. Verified savings: about 3,730 words, roughly 11 typeset pages net of the reminders.

**16. [CUT-REPETITION] must — Section 3.2.3 "The `beta`-mixing coefficient", Definition `def:beta` and the two paragraphs that follow (lines 78–94, 207 words).** Home: Definition `def:prelim-ab-sigma` and Definition `def:prelim-mixing` of Chapter 2, whose text already carries the non-stationary supremum, the monotonicity in `k`, and the comparison with Yu's stationary coefficient; the inheritance claim of line 92 is Lemma `lem:prelim-inherit`. Keep one sentence: "Throughout, `beta(k)` is the non-stationary `beta`-mixing coefficient of Definition~\ref{def:prelim-mixing} of Chapter~\ref{ch:prelim}, and the block scores inherit the coefficients of the state process by Lemma~\ref{lem:prelim-inherit}."

**17. [CUT-REPETITION] must — Lemma `lem:disint` with its proof (lines 96–109, 306 words).** First words of the passage: "Let `Y,Z` be as in Definition~\ref{def:beta} and let `kappa(y,\cdot)` be a regular conditional distribution". Home: Lemma `lem:prelim-beta-tv` of Chapter 2, proof in Appendix~\ref{app:proofs} Section~\ref{appB:betatv}. The disintegrated form `beta = E[dTV(P_{Y|X}, P_Y)]` is stated there verbatim. Keep one sentence at the point of use in the proof of Theorem `thm:cov`(ii), not here: "the coefficient is the expected conditional total-variation discrepancy (Lemma~\ref{lem:prelim-beta-tv})". Delete the "Two equivalent forms" paragraph at line 94 with it.

**18. [CUT-REPETITION] must — Remark "Total variation and Kolmogorov distance" (lines 111–113, 118 words).** First words: "For laws `P,Q` on `R` with distribution functions `F_P,F_Q`, the Kolmogorov distance is". Home: Definition `def:prelim-dK-dTV` and Proposition `prop:prelim-dK-le-dTV` of Chapter 2, which give the definition, the inequality `dK <= dTV`, and a worked example of strictness. This item is **not** on the Chapter 2 panel's list of twelve; it is a thirteenth. Keep the one sentence that is Chapter 3's own and not in Chapter 2 — "the certificate evaluates the deployment law only on half-lines, which is why the drift penalty is `dK` and not `dTV`" — with the cross-reference.

**19. [CUT-REPETITION] must — Lemma `lem:berbee` with its 599-word proof and its two framing paragraphs (lines 155–184).** First words of the passage to delete: "Let `Y,Z` be random elements of Polish spaces on a probability space that also carries a random variable `U` uniform on `(0,1)`". This result currently has **three** homes: Lemma `lem:prelim-berbee` in Chapter 2, the full proof in Appendix~\ref{app:proofs} Section~\ref{appB:berbee} (which contains the same maximal-coupling construction with the Borel-isomorphism selection), and this statement-plus-proof. The chapter's own text at line 160 admits it: "It is restated in Chapter~\ref{ch:prelim} and appears again ... in Appendix~\ref{app:proofs}." Delete the statement, the proof, the sentence beginning "The lemma is due to `\cite{berbee1979}`" (Chapter 2 carries the attribution), and the closing paragraph "The value of the auxiliary variable is that ...". Keep one sentence in Step 1: "Step 1 applies Berbee's coupling lemma (Lemma~\ref{lem:prelim-berbee}) once per block, in time order."

**20. [CUT-REPETITION] must — Lemma `lem:blocks` with its proof (lines 185–212, 413 words), subject to item 3.** First words: "Let `(X_t)` have `beta`-coefficients `beta(\cdot)`, and let `G_1,\dots,G_m` be index sets of the form". Home: Lemma `lem:prelim-yu` of Chapter 2 (stated abstractly in terms of `beta(sigma(X_{1:j-1}), sigma(X_j)) <= beta_j`, which is exactly the form Chapter 3 needs), proof in Appendix~\ref{app:proofs} Section~\ref{appB:yu}, blocking construction in Algorithm `alg:prelim-blocking`. Delete also the preamble at line 183 ("The statement below is the coupling form of Lemma~4.1 of `\cite{yu1994}`, whose statement bounds ..."), which Chapter 2's Remark `rem:prelim-yu-original` already carries. **Keep** Corollary `cor:coupled`, which is Chapter 3's own bookkeeping, and keep the one gap computation that Appendix B does *not* cover: Appendix B's proof derives `beta_j <= beta(b+1) <= beta(b)` from a layout with `b+1` indices of separation, whereas this chapter's absorbed-endpoint layout has `s_{i+1} = t_i + b` exactly, so the verification `beta_j <= beta(X_{1:t_{i-1}}, X_{t_{i-1}+b:\infty}) <= beta(b)` must survive, in two lines, inside `cor:coupled`. Keep the sentence "the coupling form implies Yu's expectation bound, since `|E h(Xi) - E h(Xi^*)| <= ||h||_infty Prob(Xi != Xi^*)`" only if Chapter 2's Remark `rem:prelim-yu-original` does not already say it; it does, so delete it.

**21. [CUT-REPETITION] must — Lemma `lem:beta` with its 534-word proof and its preamble (lines 226–258).** First words of the proof to delete: "Let `V_1,\dots,V_n` be i.i.d. uniform on `(0,1)` and independent of the `T_i`, and define the randomised probability integral transforms". Home: Theorem `thm:prelim-beta` of Chapter 2, which now states the atom case as stochastic domination and proves it with the identical randomised transform, and Appendix~\ref{app:proofs} Sections~\ref{appB:pit} and~\ref{appB:orderstat} for the transform and the order-statistic density. Keep one sentence in Step 2: "By Theorem~\ref{thm:prelim-beta} of Chapter~\ref{ch:prelim} — the Beta form of `\citep[Prop.~2b, p.~479]{vovk2012}`, stated there in the generality that allows atoms — `F_P(q̂^*)` stochastically dominates `Beta(k, n+1-k)`, with equality in law when `F_P` is continuous." **Keep** Corollary `cor:exact`, which specialises it to the coupled sample given `D_fit` and is Chapter 3's own step.

**22. [CUT-REPETITION] must — Lemma `lem:hoeffding` with its proof (lines 275–291, 180 words).** First words of the proof: "For `lambda >= 0` let `Lambda(lambda) := log E e^{lambda(B_1-u)}`". Home: Lemma `lem:prelim-hoeffding` of Chapter 2, proof in Appendix~\ref{app:proofs} Section~\ref{appB:hoeffding}. Chapter 2's version is stated in the sharper `kl`-divergence form with the sub-Gaussian bound as a corollary, which is strictly more than Chapter 3 uses.

**23. [CUT-REPETITION] must — Corollary `cor:dkw` with its proof (lines 293–306, 408 words), including the "DKW form as a corollary" paragraph.** First words: "The bound `b_{n,k}(delta) >= 1 - alpha - sqrt(log(1/delta)/(2n))` in Theorem~\ref{thm:cov}(ii) is what the one-sided Dvoretzky--Kiefer--Wolfowitz inequality". Home: Corollary `cor:prelim-beta-bound` of Chapter 2. Keep in Theorem `thm:cov`(ii) the inequality itself, with "(Corollary~\ref{cor:prelim-beta-bound} of Chapter~\ref{ch:prelim}; `\citep[Prop.~2a, p.~478]{vovk2012}`)" — see item 13. The paragraph "The Gaussian-tail approximation" at line 304 (215 words) also duplicates Chapter 2's `eq:prelim-beta-normal` and `ex:prelim-beta-numbers`; cut it to two sentences and keep only the `n = 25` numbers, which are this chapter's own worked example.

**24. [CUT-REPETITION] must — Definition `def:wass` (lines 402–404).** Home: Definition `def:prelim-W` of Chapter 2, identical in content. Delete; cite.

**25. [CUT-REPETITION] must — Lemma `lem:w1area` with its proof (lines 406–416, 157 words).** First words of the proof: "Only the inequality is used below, and it needs no duality theory." Home: Theorem `thm:prelim-w1-identity` of Chapter 2, proof in Appendix~\ref{app:proofs} Section~\ref{appB:w1} — which proves the identity in both directions and the quantile form, i.e. strictly more. Chapter 2's own commentary already states the consequence Chapter 3 wants: "a Kolmogorov gap of size `d` at a point forces, when `F_Q` is `L`-Lipschitz, a triangle of area `d^2/(2L)`". Delete; cite in the proof of `prop:w1`.

**26. [CUT-REPETITION] must — Lemma `lem:winf` with its proof (lines 418–424, 168 words).** First words of the proof: "The monotone coupling has `esssup|F_P^{-1}(U) - F_Q^{-1}(U)| = sup_u |F_P^{-1}(u) - F_Q^{-1}(u)|`". The `Winf` clause of Proposition `prop:w1` needs only the shift property `F_P(x) <= F_Q(x + Winf)`, which is Proposition `prop:prelim-winf-shift` of Chapter 2 — and Chapter 2 already spells out the very consequence, "If `Q` has a density bounded by `L`, the proposition gives `F_P(x) - F_Q(x) <= L Winf(P,Q)`, the `Winf` bound of Proposition~\ref{prop:w1} of Chapter~\ref{ch:certification}". Delete the quantile identity from Chapter 3 and replace the `Winf` paragraph of the proof of `prop:w1` by one line citing `prop:prelim-winf-shift`. Do **not** ask Chapter 2 to add the quantile identity: nothing in the thesis uses it.

**27. [CUT-REPETITION] must — Lemma `lem:bl` with its proof (lines 472–491, 316 words), and the `(2+eps)`-moment paragraph that follows.** First words of the proof: "By Lemma~\ref{lem:w1area} (with equality, or with the inequality in the direction needed here supplied by the same monotone coupling)". Home: Theorem `thm:prelim-bobkov` of Chapter 2, stated there with its moment conditions, and Chapter 2's commentary at its line 1136 already derives the `m^{-1/4}` consequence for the plug-in. Keep one sentence in Section 3.4.4: "On the coupling event the calibration scores are i.i.d. `P` given `pi`, so `E[W_1(P_n, P) | D_fit] <= J_1(P)/sqrt(n)` by Theorem~\ref{thm:prelim-bobkov} of Chapter~\ref{ch:prelim}, with `J_1(P)` finite under a finite `(2+eps)`-th moment." **Keep** Corollary `cor:plugin`, which is Chapter 3's own.

**28. [CUT-REPETITION] must — Section 3.7.1 "An explicit mixing bound for Gaussian AR(1)" in its entirety: Lemma `lem:markovbeta` with proof, Lemma `lem:ar1` with proof, and the framing sentences (lines 590–627, 403 words).** First words of the subsection: "Let `X_t = varphi X_{t-1} + sigma zeta_t` with `|varphi| < 1`". Home: Lemma `lem:prelim-markov` of Chapter 2 (the Markov reduction, identical statement and proof route) and Proposition `prop:prelim-ar1` of Chapter 2, which the Chapter 2 reviser has already rewritten to carry precisely this chapter's chain of three inequalities with no side condition and the constant `C = 1/(2 sqrt(1-phi^2))`. Note in particular that the sentence "Proposition~\ref{prop:prelim-ar1} of Chapter~\ref{ch:prelim} proves that it is geometrically `beta`-mixing with `beta(k) <= |varphi|^k/sqrt2` for `varphi^{2k} <= 1/2`" is now factually wrong about Chapter 2 and must go with the rest. Replace the subsection by two sentences at the head of Section 3.7: "Let `X_t = varphi X_{t-1} + sigma zeta_t` be the stationary Gaussian AR(1) with `varphi = 1/2` and stationary variance `v = 1` (innovation standard deviation `sqrt(1-varphi^2)`). Proposition~\ref{prop:prelim-ar1} of Chapter~\ref{ch:prelim} gives `beta(k) <= (1/2) sqrt(-log(1-varphi^{2k}))`, which at `k = 10` is `2^{-11} = 4.883e-4` to four significant figures." Retain Proposition `prop:noest`'s use of `lem:prelim-berbee` (item 30).

### Positioning, citation and clarity

**29. [ADD] should — Section 3.1 (line 22) and Section 3.9 Chapter notes (line 756): three sentences that complete the positioning of Theorem `thm:cov`(i).**
Judged against the Highlights notes, the positioning is substantially honest already: the abstract, the introduction and the chapter notes each say that (i) is a blocked variant of `oliveira2024` and is dominated for marginal coverage by `barber2026`, which is what GROUND-TRUTH correction 12 and the conformal referee's lesson require. Three gaps remain. (a) Say that Oliveira et al.'s *algorithm* is unblocked and that their blocking lives in the proof: "`\citet[p.~2]{oliveira2024}` emphasise that their predictor `avoid[s] intensive routines such as bootstrapping, ensembling or blocking' and is `exactly the same as the one used for the iid data'; the blocking is a device inside their proof `\citep[Prop.~10, pp.~20--21]{oliveira2024}`, whereas the certificate here blocks the scores themselves, which is what makes the calibration-conditional statement available." (b) State their constant beside the chapter's: their feasible set requires `delta_cal > 4(m-1)beta(a) + beta(r)` `\citep[eq.~(12), p.~7]{oliveira2024}`, against `(n+1)beta(b)` here — the factor is the price of using both odd and even blocks, per `\citep[Lemma~4.2, p.~103]{yu1994}`. (c) State explicitly that `barber2026` contains no calibration-conditional statement (the `barber2026` Highlights note records "all results are marginal"), and that their own discussion leaves open "whether split conformal is optimal for this class of problems" `\citep[Sec.~4]{barber2026}`. This is the positive case for the blocked route and it is currently only implied.
Why: honesty perspective and the reader's need — the chapter asks the reader to accept a route it has just declared dominated, and the three sentences are the whole justification. Priority **should**.

**30. [FIX] should — Section 3.1 (line 22) and Chapter notes (line 756): cite the right corollary of `barber2026`.**
Both places cite `\cite[Cor.~1--2]{barber2026}` for a bound "`2beta(tau)+2beta(tau^*)` plus a term linear in `tau/n`". The `2beta(tau)+2beta(tau^*)` form is Corollary 2, the split/trained-score case; Corollary 1 is the pretrained case with cost `2beta(tau)` alone. Chapter 3's setting has a policy fitted on `D_fit`, i.e. a trained score, so the comparison object is `\citep[Cor.~2]{barber2026}` and it should be cited alone where the two-term form is quoted, with Corollary 1 mentioned only for the pretrained case. Keep `\citep[Thm.~2]{barber2026}` for tightness, and keep the chapter's correct qualification that Theorem 2 is a construction for split conformal, not a minimax bound over procedures. Priority **should**.

**31. [FIX] should — Section 3.5.1, proof of Proposition `prop:noest`, the upper bound in (c) (line 536).**
The step "on the complementary event the future `X_{t+k:\infty}` is a function of the refresh variables alone and hence independent of `X_{1:t}`" is not a proof as written: the event is itself measurable with respect to the refresh variables, so conditioning on it does not leave the future distributed as its marginal. Make the coupling explicit in three lines: run a second copy of the chain from an independent draw of the invariant law on `{0,1}^N`, driven by the *same* `(xi_t, zeta_t)` for `t > N`; the two copies agree from time `t+k` onwards off the event that some coordinate lineage went `floor(k/N)` consecutive steps without a refresh, and that event has probability at most `N(1-eps)^{floor(k/N)}`; the coupling inequality of Lemma `lem:prelim-berbee` (or directly `dTV <= Prob(disagreement)`) then gives `beta_2(k) <= N(1-eps)^{floor(k/N)}`. Note that the sentence "Lemma~\ref{lem:berbee} in reverse" must in any case be re-pointed to `lem:prelim-berbee` after item 19.
Why: proof-step-checker standard; the conclusion is true and the fix is short, but as drafted the step would not survive a careful reader, and this proposition is one of the chapter's five claimed additions. Priority **should**.

**32. [CLARIFY] should — Theorem `thm:cov`(ii), statement (line 133), and the assembly at line 388.**
The statement splits the conclusion across two events: "with probability at least `1-delta-n beta(b)` over `(D_fit, S_{1:n})` ... the last term holding on an event of probability at least `1-delta'`". Both events are in fact `sigma(D_fit, S_{1:n})`-measurable — `D = dTV(kappa(X_{1:t},\cdot), mu_0)` is `sigma(X_{1:t})`-measurable and `E[D | G]` is `G`-measurable by construction — so the statement should be a single one: "with probability at least `1 - delta - delta' - n beta(b)` over `(D_fit, S_{1:n})`, `Prob(S_0 <= q̂ | D_fit, S_{1:n}) >= b_{n,k}(delta) - dK(P,Q) - beta(b)/delta'`." Merge them, and note that this is exactly the number the worked example already reports (`1 - 0.05 - 0.05 - 0.0122 = 0.8878`), which currently appears as an unexplained "joint event" figure.
Why: an examiner reads every clause of a theorem as a claim, and a conclusion stated on two unnamed events invites the question whether they are the same event; the mathematics already answers it. Priority **should**.

**33. [CLARIFY] should — Section 3.3.4, deployment side of the proof (line 380).**
The parenthetical "because `X_{H_0}` (left endpoint included) is a function of `X_{t+b:\infty}`, the deployment block beginning at least `b+1` periods after `t`" mixes the two indexings: with the left endpoint absorbed, `X_{H_0}` begins at index `N_0+b+N >= t+b`, which is the gap-`b` coefficient of Definition `def:prelim-mixing`; in the unabsorbed indexing the first *period* of `H_0` is `t+b+1`. State one indexing and use it: "with the left endpoint absorbed, `X_{H_0}` begins at index `N_0+b+N >= t+b`, so `X_{H_0}` is a function of `X_{t+b:\infty}` and the coefficient is `beta(b)`." Corollary `cor:coupled` already states it correctly, so the two should agree word for word.
Why: an off-by-one in the gap silently changes `beta(b)` to `beta(b+1)` and breaks the hypothesis of the coupling lemma; the chapter has the right inequality and the wrong sentence. Priority **should**.

**34. [REMOVE] should — Section 3.7.3, Table `tab:example` columns `b = 5` and `b = 20`, the paragraph "Reading the table" (line 700), the `phi = 0.8` discussion, and Figure `fig:example` with its `\datanote{}` (lines 702–706).**
Move all of it to Chapter 4. Keep in Chapter 3 the single-`b` certificate: the `b = 10` column of the table (reduce the table to one column, or fold it into the running text, which is already complete), the drift-slack computation, the exact-versus-approximate quantile comparison, and one sentence — "The block length is the design variable of the method; its choice within a mixing class, and the trade-off between the coupling term and the number of blocks, is the subject of Chapter~\ref{ch:sharpness}." The b-sweep from `b = 2` to `50` at two values of `phi`, the vacuity of the certificate at `phi = 0.8`, `b = 10`, and the Monte Carlo breach frequencies belong where blocking is studied.
Judgement asked for: **the worked example belongs in Chapter 3, but only at a single block length.** STANDARD requires a worked example in every chapter that admits one, and Chapter 3's example is what shows the certificate is computable by hand — it must stay. But a geometrically mixing AR(1) lies in *every* class `B_r`, so a sweep over `b` on this process illustrates nothing about the optimal `b^*`, whose minimiser depends on `r` and not on the process; presenting the three columns as a block-length study in Chapter 3 invites precisely the misreading that `b = 10` is optimal in some sense, when the smallest `b` that kills the coupling term is all that is happening. Chapter 4, which fixes `r`, is where a sweep means something. Priority **should**.

**35. [CITE] should — Section 3.9 Chapter notes: two papers that belong in the related-work map and are absent.**
(a) `stocker2025`: add one sentence distinguishing the certificate from the construction the survey calls SCP-block — "The blocked construction here should not be confused with split block-permutation conformal prediction, which redefines the unit of exchangeability rather than the score `\citep[§3, p.~7]{stocker2025}`, loses exact finite-sample validity `\citep[§3.4, p.~13]{stocker2025}`, and undercovers even in stationary simulations `\citep[p.~15]{stocker2025}`." Without it a reader who knows the survey will import a negative empirical finding about a different method. (b) `schmitt2026`: add one sentence as the nearest finance application — "In a value-at-risk setting `\citet[Thm.~1, p.~3]{schmitt2026}` obtain a coverage bound with a coefficient `2` on a total-variation drift term under a weighted-calibration argument, and show the factor `2` is not removable for that argument `\citep[p.~3]{schmitt2026}`; the coupling route here charges `dK` with coefficient `1`, which Chapter~\ref{ch:sharpness} shows to be sharp." Locators confirmed in both Highlights notes. Priority **should**.

**36. [CLARIFY] should — Section 3.3, line 131.** The chapter re-defines `b_{n,k}(delta) := Beta^{-1}(delta; k, n+1-k)` immediately before Theorem `thm:cov`; Chapter 2's `eq:prelim-beta-quantile` already defines it. Replace by "with `b_{n,k}(delta)` the Beta quantile of \eqref{eq:prelim-beta-quantile}". Priority **should**.

**37. [ADD] should — Remark `rem:scoremap` (line 74) or Section 3.7.** One sentence on atoms in the worked example: the example's score law is continuous, so `F_P(q̂^*)` is exactly `Beta(24,2)` and the reported numbers are exact; on a real hedging record with rounded profit-and-loss the law has atoms and the same numbers become lower bounds by the domination of Theorem `thm:prelim-beta`. The chapter takes trouble to secure the atom case and then never says where it bites. Priority **should**.

---

## 4. Missing for a strong doctoral chapter

What a doctoral examiner will look for and not find is not more machinery but the **other side of the inequality**: the chapter proves that coverage cannot fall much below `1-alpha` and never asks how far above it can sit, so the two-sided calibration-conditional error `|F_P(q̂)-(1-alpha)|` — the object GROUND-TRUTH correction 11 makes the minimax quantity of the doctoral theorem in Chapter 4 — is never defined in the chapter that supplies Chapter 4's tool (item 1). Add it and the chapter becomes the natural antecedent of Theorem 9 rather than a one-sided lemma-store. Two smaller absences complete the picture: the deployment-side Wasserstein error is currently disposed of in prose and needs a genuine `\gap{}` with the Dedecker–Merlevède locators, so that a reader can see exactly which inequality is missing and where it will come from (item 15); and the positive case for the blocked route — that Barber and Pananjady have no calibration-conditional statement at all, that Oliveira et al.'s algorithm is unblocked and their constant is four times larger, and that the open question their discussion poses is the one Chapter 4 answers — needs three sentences of explicit statement rather than the current implication (item 29). Beyond those, nothing should be added. After the thirteen repetition cuts the chapter runs about forty typeset pages against a budget of forty-four, and items 1, 15 and 29 consume most of the remaining four; the chapter is not short of substance, it is short of space.

## 5. Should go

Roughly seventeen pages of the fifty-one should leave the chapter and only the certificate should remain. Eleven of them are results with a home elsewhere: Berbee's lemma (three homes at present), Yu's sequential coupling, the disintegration of `beta`, the Beta law with atoms, Hoeffding, the Beta-quantile corollary, the two Wasserstein definitions, the area identity, the `Winf` quantile identity, Bobkov–Ledoux, the Markov reduction and the AR(1) bound — items 16 to 28, every one of which is now stated in Chapter 2 in exactly the generality this chapter uses, nine of them with their proofs in Appendix B, the Beta law and the AR(1) bound having been strengthened by the Chapter 2 reviser for this very purpose. Six more pages are Section 3.8's descriptions of the moving-block bootstrap, the stationary bootstrap recipe, the Kupiec and Christoffersen statistics and the Basel traffic light: every object there is used only by Chapter 6, and the unverified Basel zone boundaries should not sit in a theory chapter at all (item 4). About a page and a half more is the block-length sweep in the worked example, which means nothing on a geometrically mixing process and belongs where `r` is fixed (item 34). And half a page is narration of the drafting process — the `\gap{}` box that argues with a document the reader has never seen, the two "first draft" confessions, the dropped-theorem announcement and the recomputation log in the table's `\datanote{}` — which the invisible-apparatus rule removes outright (item 7). Keep every one of the chapter's own objects: `def:score`, `ass:mix`, the fixed-policy laws, `thm:cov` and its assembly, `cor:coupled`, `cor:exact`, `lem:halfline`, `rem:jointtv`, `rem:sequence`, `prop:w1` with its counterexample, `cor:plugin`, `prop:noest`, `alg:cert`, and the `b = 10` certificate computed by hand.

## 6. Lessons enforced

- `[2026-09-09][orchestrator][all]` never mention council, ground truth, lessons, drafts or versions — item 7 (six loci), item 2.
- `[2026-09-09][review-panel][ch2,all]` a chapter must never name the project's drafting apparatus — item 7.
- `[2026-09-09][orchestrator][all]` single-source rule; the ch3 panel must demand removal of restated ch2 material — items 16–28.
- `[2026-09-09][review-panel][ch2,ch3]` ch2 must state each tool in the generality ch3 uses (Beta law with atoms, AR(1) bound without a side condition, non-stationary `beta` with Yu's stationary definition named) — verified done in ch2; items 21 and 28 therefore unblocked and made must.
- `[2026-09-09][review-panel][ch2,appB]` the nine moved proofs live in App B under "Proof of <result>" — items 19, 20, 21, 22, 25, 27 checked against `appB:berbee`, `appB:yu`, `appB:pit`, `appB:orderstat`, `appB:hoeffding`, `appB:betatv`, `appB:w1`.
- `[2026-09-09][reviewer-conformal][ch3,ch4,ch6]` nothing asymptotic inside a theorem statement — verified closed in the chapter (Remark `rem:betaapprox`); item 2 removes the residual narration of the move.
- `[2026-09-09][reviewer-conformal][ch3]` Assumption is read as `beta`-mixing of the STATE process — item 2, now that GROUND-TRUTH correction 15 has been made.
- `[2026-09-09][reviewer-conformal][ch3,ch4]` with the left endpoint absorbed, `s_{i+1} = t_i + b` exactly and the deployment side goes through `X_{t+b:\infty}`, not `X_{t+b+1:\infty}` — items 20 and 33.
- `[2026-09-09][reviewer-conformal][ch3]` the randomised PIT gives domination in the direction a coverage lower bound needs, with the half-line closed — verified correct in `lem:beta`; item 21 moves it to its home without weakening it.
- `[2026-09-09][reviewer-conformal][ch3]` part (ii) holds on a `sigma(D_fit,S_{1:n})`-measurable event — verified correct; item 32 merges the two such events.
- `[2026-09-09][reviewer-conformal][ch3,ch1,ch8]` Thm 6(i) is a blocked variant of Oliveira et al., dominated for marginal coverage by Barber–Pananjady; the chapter's additions are confined to (ii), the joint-TV remark, the fixed-policy accounting, Prop `prop:w1` with its counterexample, and the non-estimability construction — verified present; items 6, 29, 30 complete it and item 6 removes `prop:w1`'s inequality from the list of additions.
- `[2026-09-09][reviewer-conformal][ch3,ch4]` never write "matching minimax lower bound" or "rate-optimal" for `N^{-r/(2r+3)}` — verified: the chapter says "blocking buys the exact Beta law and a failure probability depending on `(C,r)` alone, not rate". Closed.
- `[2026-09-09][reviewer-conformal][ch3]` never cite the source-of-record document or its numbering inside a chapter — item 2, item 7.
- `[2026-09-09][reviewer-conformal][ch3]` cite the source for attribution and cross-reference the ch2 statement rather than inventing theorem numbers — items 13, 22, 23.
- `[2026-09-09][reviewer-conformal][ch3,appC,ch2]` three valid bounds on `beta(10)` coexist and must be quoted with their source — item 14, updated for the code's current state.
- `[2026-09-09][code-conformal][ch3,appC]` the code now implements `lem:ar1`'s first inequality and reproduces every cell of Table 3.1 at `mu = 0.05`; the datanote's `0.8853/0.8126/0.9359` sentence is stale — item 14.
- `[2026-09-09][code-conformal][ch3,ch4]` block count is `n = floor((N-s_0)/(2b))`; ch3's `250+240+10 = 500` layout is the correct one — verified.
- `[2026-09-09][code-conformal][ch4]` quote the exact minimiser `(2(r+1))^{2/(2r+3)} N^{3/(2r+3)}`, never the bare balance — item 11.
- `[2026-09-09][code-conformal][ch3,ch4]` do not present the AR(1) example as an illustration of `b^*`, since geometric `beta` lies in every `B_r` — item 34.
- `[2026-09-09][code-conformal][ch3,ch6]` report `k/(n+1)` alongside `1-alpha` when `n` is small — verified present (`24/26 = 0.9231`).
- `[2026-09-09][code-conformal][ch3,ch6]` the stationary bootstrap median quantile is a point estimate, never a competitor prediction bound or a theorem — verified: the chapter claims no theorem; item 4 relocates the description.
- `[2026-09-09][code-conformal][appC]` state the variance convention (`v = 1`, innovation sd `sqrt(1-phi^2)`) with every AR(1) figure — item 14.
- `[2026-09-09][reference-librarian][ch3,ch4]` cite `adams2010` as `\citep[Sec.~1.1]{adams2010}` for "no distribution-free rate under ergodicity alone"; soften the non-estimability wording — item 10.
- `[2026-09-09][reference-librarian][ch3,ch4]` `barber2026` locators; the paper has no calibration-conditional statement — items 29(c), 30.
- `[2026-09-09][reference-librarian][ch2,ch3]` `barber2023` locators; no `beta`-mixing and no Beta law in the paper — item 1 (Thm 3 as the over-coverage precedent).
- `[2026-09-09][reference-librarian][ch4]` `dedecker2017` locators for the dependent empirical-`W_1` bound — item 15.
- `[2026-09-09][reference-librarian][ch3]` `allohibi2026` note that buffer-free marginal validity is already given by Oliveira et al. and Barber–Pananjady — verified present in the chapter notes.
- `[2026-09-09][reference-librarian][all]` no verified locator exists for `berbee1979` or `bobkov2019`; cite them without a locator and carry the proof in ch2/App B — items 19 and 27 do exactly that.
- STANDARD, zero-hallucination citation rule — items 6, 10, 12, 13, 15, 35; and the finding that `hoeffding1963` and `massart1990` are in `bibliography.tex` with **no Highlights note on disk**, so no locator for either may be quoted in this chapter (item 13).
- STANDARD, chapter template (worked example required) — item 34, which keeps the example in Chapter 3.
- STANDARD, page budget (Ch3 = 44) — items 4, 16–28, 34; measured 15,203 words ≈ 51 pages before cuts, ≈ 40 pages after, leaving room for items 1, 15 and 29.
- STANDARD, canonical chapter labels — verified: the chapter uses `ch:prelim`, `ch:sharpness`, `ch:identifiability`, `ch:usd`, `ch:zar`, `app:proofs`, `app:data` throughout.
- GROUND-TRUTH corrections 4, 5, 9, 10, 11, 12, 14, 15, 16 — items 1, 2, 6, 10, 11, 12; corrections 4 and 5 verified satisfied.
